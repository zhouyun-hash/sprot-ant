-- =============================================================================
-- Docker MySQL 首次初始化时执行（仅新建数据卷时跑一遍）
-- =============================================================================
-- 请勿在此维护完整 DROP/CREATE：易与 TypeORM 实体脱节。
-- 实际表结构由后端脚本根据实体自动创建/补列：
--   npm run db:migrate:compat --prefix backend
--   （根目录可执行：npm run db:migrate）
-- 默认管理员账号在迁移脚本末尾写入（若尚不存在）。
-- =============================================================================

SET NAMES utf8mb4;
